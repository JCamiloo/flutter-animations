import 'package:flutter/material.dart';
import 'package:headers/src/widgets/headers.dart';

class HeadersScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: GradientWavesHeader(),
    );
  }
}